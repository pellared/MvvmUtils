using System;

namespace Pellared.Utils
{
    public static class StopwatchExtensions
    {
        //public static IDisposable Measurement(this Stopwatch stopwatch)
        //{
        //    stopwatch.ThrowIfNull("stopwatch");

        //    stopwatch.Stop();
        //    stopwatch.Reset();
        //    stopwatch.Start();
        //    var stopper = new DisposeAction(stopwatch.Stop);
        //    return stopper;
        //}

        //public static IDisposable Continue(this Stopwatch stopwatch)
        //{
        //    stopwatch.ThrowIfNull("stopwatch");

        //    stopwatch.Start();
        //    var stopper = new OnLeaveAction(stopwatch.Stop);
        //    return stopper;
        //}
    }
}